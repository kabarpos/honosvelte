# Audit Remediation Evidence Ledger

Every status below is backed by tests and/or documented evidence. A status is
only raised when the corresponding regression guard exists.

| ID | Status | Evidence | Last verified |
| --- | --- | --- | --- |
| GOV-01 | complete | `docs/security/threat-model.md` defines trust boundaries, data classification, security invariants, abuse cases, and deployment assumptions. | 2026-08-14 |
| GOV-02 | complete | `tests/security/helpers.ts` provides reusable app-call, session-cookie, JSON, redirect, and response-payload assertions. Suites are split `tests/security/*.test.ts` (15 files) and `tests/correctness/*.test.ts` (4 files); UX-contract browser checks live in `e2e/a11y.spec.ts` (Playwright + axe). | 2026-08-14 |
| GOV-03 | complete | This ledger records remediation status, evidence paths, and verification dates. | 2026-08-14 |
| SEC-01 | complete | `getPublicSettings()` allowlists public settings; WhatsApp returns `hasApiKey`, never the key. `tests/security/secrets.test.ts` now also asserts: admin `/settings` payload + flash never echo secrets, and a subprocess log-capture audit proves request logs emit only `method pathname -> status` (no query/body/secret). `docs/security/secret-rotation.md` documents the operator rotation procedure (execution is deployment-owner dependent — documented limitation). | 2026-08-14 |
| SEC-02 | complete | Protected-role guards, last-super-admin protection, role-change activity, `tests/security/protected-role.test.ts`, plus full super-admin mutation coverage in `tests/rbac.test.ts` (super_admin can act on every permission, renders every admin page). Existing-data review: 1 user, 3 admins, no super_admin, no inactive user, no role-change events. | 2026-08-14 |
| SEC-03 | complete | `resolveUser`, password login, Google OAuth, and user status route enforce inactive state; deactivation deletes sessions; `tests/security/inactive-user.test.ts` and the Google OAuth section of `tests/tus.test.ts` cover login/session/reactivation/OAuth rejection. | 2026-08-13 |
| SEC-04 | complete | Password reset atomically consumes the token, deletes all user sessions, and records `password.reset`; authenticated password-change policy preserves current session and deletes other sessions. `tests/security/password-reset-session.test.ts`. | 2026-08-13 |
| SEC-05 | complete | Webhook requires `WHATSAPP_WEBHOOK_SECRET`, enforces bounded TypeBox JSON, replay window, rate limit, field limits, idempotency migration, safe response, per-phone auto-reply quota, and provider circuit breaker. `webhook.test.ts`, `webhook-rate-limit.test.ts`, `webhook-circuit.test.ts` pass. | 2026-08-13 |
| SEC-06 | complete | Tus GET private owner-scoped/completed-only, attachment-only non-image, bounded file-signature detection; media GET now also sends `X-Content-Type-Options: nosniff`. `tests/tus.test.ts` + `tests/media.test.ts` cover owner/cross-user/incomplete/HTML/mixed-case/signature-mismatch/avatar flows. | 2026-08-14 |
| SEC-07 | complete | SMTP TLS bypass dev-only + prod fail-fast; 10s timeouts; Dripsender allowlist; SSRF guard incl. DNS; manual redirects = failure; avatar 2 MiB bounded + signature; integration detail limited. `ssrf.test.ts`, `webhook-circuit.test.ts`, `tus.test.ts` (avatar). | 2026-08-13 |
| SEC-08 | complete | Capability payload `auth.can` + `src/client/capabilities.ts` is the single UI policy source; nav + 7 admin pages no longer hardcode `role === 'admin'`; route→permission mapping in `docs/security/rbac.md`; `tests/rbac.test.ts` covers payload, super_admin rendering, revoked-permission parity. | 2026-08-14 |
| SEC-09 | complete | Full-origin CSRF comparison, malformed-Origin rejection, `TRUSTED_PROXY` (IP/CIDR fail-fast), `resolveClientKey` trusts only the first `X-Forwarded-For` from trusted proxies, account-aware auth rate limit. `tests/security/csrf.test.ts` + `tests/security/rate-limit-proxy.test.ts`. | 2026-08-14 |
| COR-01 | complete | `searchUsers`/`countSearchUsers` with name/email/limit/offset; pagination + search, empty search, negative/wildcard tests; `escapeLike()` + `ESCAPE '\'` on all LIKE statements. `tests/correctness/user-search.test.ts`. | 2026-08-14 |
| COR-02 | complete | Contact search name/email/subject/message with identical count/list filters; positive/negative/wildcard tests. `tests/correctness/contact-search.test.ts`. | 2026-08-14 |
| COR-03 | complete | Media picker owner/admin scoped; cross-user metadata isolation verified. `tests/correctness/media-picker-scope.test.ts`. | 2026-08-13 |
| COR-04 | complete | Notification read update owner/broadcast scoped with affected-row check; fan-out includes super_admin. `tests/security/notification-ownership.test.ts`. | 2026-08-13 |
| COR-05 | complete | Media POST: bounded body read (no unbounded buffer), signature validation (declared image type must match magic bytes), temp-file + atomic rename, cleanup on failure, `nosniff` on serve; `reconcileMedia()` background job (dangling rows + orphan/tmp files) wired into the 15-min sweep; failure-injection + reconciliation tests in `tests/media.test.ts`. | 2026-08-14 |
| COR-06 | complete | Per-upload in-process lock + offset repair: when the on-disk file is ahead of the DB offset (interrupted write), HEAD and PATCH reconcile the DB so resuming clients don't 409-loop. Concurrent race test + interruption-recovery test in `tests/tus.test.ts` (byte-exact). Multi-process consistency is documented as a single-instance limitation (threat-model). | 2026-08-14 |
| COR-07 | complete | `ValidationFailed` handled before `logError`; background job errors (mail/whatsapp/avatar) go through structured `logErrorRaw` with `[job]` ids; remaining `console.*` are startup/build/log-driver only. `validateJson` rejects malformed JSON (400) and enforces schemas; routes return explicit statuses. `tests/security/secrets.test.ts` log audit. | 2026-08-14 |
| PERF-01 | partial | 50 MiB default, 24h expiry, per-request JSON body limit (413), per-chunk Tus cap, upload create/PATCH rate limit, provider timeouts. Per-user/global storage quota not implemented (open item). | 2026-08-14 |
| PERF-02 | complete | Tus GET streams via `Bun.file()`; creation-with-upload streams to disk chunk-bounded; media POST bounded read + async fs (no unbounded buffering, no sync fs in the request path). Memory benchmark remains an open item. | 2026-08-14 |
| PERF-03 | partial | EXPLAIN QUERY PLAN evidence in `docs/audit/query-plan.md`; indexes via migrations 0020/0021 (incl. covering `(user_id, id DESC)` for notifications); `(? = '' OR …)` pattern + FTS5 documented as open items. | 2026-08-14 |
| PERF-04 | partial | Page cap 1000 on five paginated routes. Cursor pagination / offset hardening / count optimization / snapshot consistency remain open. | 2026-08-13 |
| PERF-05 | partial | `/health*` + `/assets/*` bypass session/settings resolution; resolved user in request context; permission guards now memoize the effective permission set per request (no user re-lookup). Role-permission cache with invalidation + latency measurement remain open. | 2026-08-14 |
| PERF-06 | partial | `sweepExpired()` every 15 min with shutdown cleanup; now also purges expired sessions/reset tokens, activity rows past `ACTIVITY_RETENTION_DAYS` (default 90), and notifications past `NOTIFICATION_RETENTION_DAYS` (default off). Contact messages are intentionally never auto-deleted (business policy, tested). Outbox/retry/dead-letter remain open. | 2026-08-14 |
| ARCH-01 | partial | Authorization policy is centralized server-side (`permissionsForUser` + guards) and client-side (`capabilities.ts`); deny/grant precedence documented in `docs/security/rbac.md`. A single policy resolver object remains an open refactor. | 2026-08-14 |
| UX-01 | complete | `Modal.svelte`: focus trap, autofocus, Escape, focus restore, `aria-labelledby`/`aria-describedby`, accessible-name fallback. | 2026-08-14 |
| UX-02 | complete | `Tabs.svelte` roving tabindex + Arrow/Home/End; dropdown/menu focus + arrow keys + restore; Switch accessible name. | 2026-08-14 |
| UX-03 | complete | Field error/hint ids + snippet context; `aria-invalid` + `aria-describedby`; focus first invalid field; consumed by Profile form. | 2026-08-14 |
| UX-04 | complete | Dead global-search input removed; notification bell is now a real link to `/notifications` gated by `notifications.read`, with the badge dot driven by a real unread count (`auth.unreadNotifications`, computed only for admins). | 2026-08-14 |
| UX-05 | partial | Contrast AA (axe clean), responsive 390px smoke, loading/empty/error/success states; mobile table/action audit, icon hit targets, reduced-motion, stale-form-state and visual regression remain open. | 2026-08-14 |
| UX-06 | complete | Super-admin sees the full admin nav (capability-driven, no hardcoded role checks); `tests/rbac.test.ts` renders every admin page as super_admin (no blank page). | 2026-08-14 |
| QA-01 | partial | Typecheck 0 error/0 warning. Lint/format script, explicit-`any` removal, and documented false-positive disposition for the ast-grep `db.query` SQL-injection rule (bun:sqlite prepared statements are parameterized by design) remain. | 2026-08-14 |
| QA-02 | complete | Security regression suite: secrets in payloads, super-admin escalation, inactive login/session, reset revoke, webhook invalid/replay/rate-limit, upload cross-user/incomplete, notification IDOR, media picker ownership, SMTP insecure prod — all covered by `tests/security/*`. | 2026-08-14 |
| QA-03 | complete | Correctness regression suite: users/contact search, pagination search, empty states, invalid page/perPage, provider timeout, file-write cleanup, tus concurrent PATCH, tus recovery, expiration sweep, retention. `tests/correctness/*` + `tests/tus.test.ts`. | 2026-08-14 |
| QA-04 | partial | Playwright + axe suite `e2e/a11y.spec.ts` (10 tests: axe scans, keyboard modal/tabs/menu, responsive 390px, admin flows) — all pass. Screenshot baseline remains open. | 2026-08-14 |
| QA-05 | pending | No automated load/performance benchmark yet (open item; documented target per environment required before claiming numbers). | 2026-08-14 |
| OPS-01 | partial | Docker non-root, HEALTHCHECK via `bun -e`, `.env.production.example` without secrets, docker-compose healthcheck synced, CI `docker-build` job. Pin Bun/container image to digest remains open. | 2026-08-14 |
| OPS-02 | partial | `/health/live` + `/health/ready` (DB, migrations, storage); structured request logs with request ID; `/health/metrics` with aggregate counters (auth failures, webhook accepted/rejected, uploads, provider errors, request status classes) + latency percentiles — all no-PII. Alert thresholds/incident runbook remain open. | 2026-08-14 |
| OPS-03 | complete | `scripts/backup.ts` (VACUUM INTO snapshot + integrity check + media/uploads mirror + MANIFEST + `--keep` pruning; restore verified in a live test); `docs/audit/backup-dr.md` documents RPO/RTO, restore steps, WAL checkpoint policy, encryption, retention. | 2026-08-14 |
| OPS-04 | partial | README env table + `.env.example` synced for retention/backup keys; role/permission model (`rbac.md`), asset policy + deployment assumptions (threat-model), backup/DR, secret rotation documented. Docker/.env.production.example and scaffolder drift check remains. | 2026-08-14 |
| OPS-05 | pending | CI security gates (typecheck-zero-warning, lint/format, `bun audit`, secret scan, SAST, Docker scan, migration smoke, security/a11y suites, coverage thresholds, action pinning) — CI exists for docker-build only; open item. | 2026-08-14 |
